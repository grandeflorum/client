import { AuditWorkFlowPipe } from './audit-work-flow.pipe';

describe('AuditWorkFlowPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditWorkFlowPipe();
    expect(pipe).toBeTruthy();
  });
});
